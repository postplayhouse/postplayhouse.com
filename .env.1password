# Safe to commit: this contains only the 1Password Environment ID.
# The plugin uses the SDK with OP_SERVICE_ACCOUNT_TOKEN, so this works with the
# stable Node toolchain and does not depend on the beta `op --environment` flag.
# @plugin(@varlock/1password-plugin)
# @initOp(token=$OP_SERVICE_ACCOUNT_TOKEN, allowAppAuth=false)
# @setValuesBulk(opLoadEnvironment(fd2j6ly53dbub7h4rcovbjmtc4))
# ---
