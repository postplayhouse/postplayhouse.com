# Duplicate this file, removing the `-example` extension to work locally. For
# production, ensure that the following ENV vars are available on the server for
# server functions and at build time for all the static files that need them.
#
# In Netlify, this is all one place:
# https://app.netlify.com/sites/postplayhouse-main-site/configuration/env

# ============================================================================
# B2 STORAGE (Backblaze)
# ============================================================================

# B2 API credentials (shared across buckets)
B2_APPLICATION_KEY_ID=SOME_ID
B2_APPLICATION_KEY=SOME_KEY

# Production bucket - used when CONTEXT=production
B2_BUCKET_ID=PRODUCTION_BUCKET_ID
B2_BUCKET_NAME=postplayhouse-prod

# Test bucket - used in dev/test/preview environments
# Create a separate bucket in Backblaze B2 console for non-production use
# This prevents dev/test uploads from polluting the production bucket
B2_TEST_BUCKET_ID=TEST_BUCKET_ID
B2_TEST_BUCKET_NAME=postplayhouse-test

# ============================================================================
# AUTHENTICATION
# ============================================================================

# Need new phrases for a new year? `pnpm gen:phrases`
INDIVIDUAL_PASSPHRASES_LIST=some-phrase,another-phrase,etc-more

# Comma-separated position numbers that have admin access to approve bios
# These positions can access /admin/bios and related admin API endpoints
ADMIN_PASSPHRASE_POSITIONS=1,2,3,4

# ============================================================================
# EXTERNAL SERVICES
# ============================================================================

# GitHub - for legacy PR-based bio submission (can be removed after migration)
GITHUB_ACCESS_TOKEN=github_pat_xxxxxxxxxxxxxx

# Basecamp - for bio submission/approval notifications
# Notifications are DISABLED in non-production environments (logged to console instead)
BASECAMP_BIO_BOT_INTEGRATION_KEY=xxxxxxxxxxxxx

# Netlify webhook secret for deploy notifications
NETLIFY_WEBHOOK_SECRET=xxxxxxxxxxxxx

# ============================================================================
# NETLIFY CACHE PURGING (Production Only)
# ============================================================================

# These are only used in production to purge CDN cache after bio approvals
# In dev/test, cache purge is simulated (logged to console)

# Get token from: Netlify User Settings > Applications > Personal Access Tokens
NETLIFY_API_TOKEN=nfp_xxxxxxxxxxxx

# Get site ID from: Site Settings > General > Site ID (or `netlify sites:list`)
NETLIFY_SITE_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# ============================================================================
# MONITORING
# ============================================================================

# Sentry - for error tracking
# The SENTRY_AUTH_TOKEN variable is picked up by the Sentry Build Plugin.
# It's used for authentication when uploading source maps.
SENTRY_AUTH_TOKEN=sntrys_xxxxxxxxxxxxxxxxxxxxxxxxxx
